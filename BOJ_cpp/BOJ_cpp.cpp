#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL), cout.tie(NULL);

	//int n, m;
	//cin >> n >> m;
	//vector<vector<int> > answer(n, vector<int>(n,0));

	//for (int i = 0; i < m; i++)
	//{
	//	int a,b,c;// 버스의 정보는 버스의 시작 도시 a, 도착 도시 b, 한 번 타는데 필요한 비용 c
	//	cin >> a >> b >> c ;
	//	arrPoint[i].x = a;
	//	arrPoint[i].y = b;
	//	++i;
	//	arrPoint[i].x = c;
	//	arrPoint[i].y=d;
	//}

	//for (int i = 0; i < n; i++)
	//{

	//	cout << answer[i] << answer[] << "\n";
	//}
}